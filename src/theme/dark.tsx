import { createTheme } from "@mui/material";

export const dark_theme = createTheme({
    palette: {
      mode:'light',
      primary: {
        main: "#FFFFFF"
      },
      secondary: {
        main: "#8F8F8F",
      },
    },
  });